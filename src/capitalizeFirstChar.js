// Write a JavaScript function that returns the Fibonacci sequence up to a given number of terms.

module.exports = function capitalizeFirstChar(str) {
  return str.replace(/\b\w/g, (l) => l.toUpperCase());
};
